# Jewerly-shop
Jewerly shop website

When running this application the program needs the following argument to work(application.properties will derive its JDBC values from application-local.properties)-->
--spring.profiles.active=local