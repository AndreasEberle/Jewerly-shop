# Password Hash Generator Application Properties

# Server configuration
server.port=8081
server.servlet.context-path=/password-hash

# Logging configuration
logging.level.com.jewelryshop.password=DEBUG
logging.level.org.springframework.security=DEBUG

# Application information
spring.application.name=password-hash-generator
spring.application.description=Standalone application for generating BCrypt password hashes

# Security configuration (disable for this utility)
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN


